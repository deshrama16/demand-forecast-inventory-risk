# Data
The raw Rossmann Store Sales dataset is not included in this repository because of its size.

The project uses the public Rossmann Store Sales dataset available through Kaggle.

The CSV files in this folder contain processed outputs generated by the forecasting and inventory-risk pipeline and are used by the Streamlit dashboard.
